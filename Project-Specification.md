# iShot Save by Date Sequence 项目说明书

## 项目名称

## **ISHOT SAVE BY DATE SEQUENCE**

在使用 Codex CLI 的过程当中，经常会遇到截屏图片发送给命令行界面中读取的情况。目前的 Codex CLI无法实现通过 iShot app截屏复制到剪贴板后粘贴到命令行界面当中，给日常工作带来了非常不必要的麻烦。因此，为了更高效地将 ishot app 与 Codex cli连接起来，考虑实施该项目。

## 项目目标

使用 iShot app 截图后，图片保存到 `~/Downloads/<year>/Images`；其中 `<year>` 为截图当年的四位年份，应自动随年份变化。例如在 2026 年，保存位置为 `~/Downloads/2026/Images`。截图名称的命名规则为：**截图当日的日期和序号**。以 2026 年 9 月 23 日为例，截图名称为 `09-23-01`、`09-23-02`、`09-23-03` 等。截图保存并更改文件名称后，自动在 Finder 中显示文件，供用户手动拖拽到 Codex CLI 使用。

将截图发送给 Codex 前，用户必须确认其中不含个人信息、密码、API key、access token、账号页面、订单、邮件或其他敏感内容。若有敏感内容，应先裁剪或打码；无法安全处理时，不应发送该截图。

## 项目步骤
- 调研阶段：通过对 iShot app、本地 Mac 帮助文档、相关网站及相似项目的研究，评估项目可行性、主要风险、实施方法和步骤，并形成明确结论和可执行建议。
- 实施阶段：根据调研结论开展建设。本仓库的 `AGENTS.md` 是项目协作与安全规则的唯一准则。若参考仓库外的文档，必须先审阅相关具体内容，并确认其不与 `AGENTS.md` 冲突。每个重要步骤和关键节点应使用 Git 版本控制；远程仓库为 `https://github.com/xiaolongwang-jpfund/ishot-save-by-date-sequence`。
- 测试阶段：按照软件工程的要求，对功能实现情况进行测试。针对存在的问题研究解决方案并执行；执行结果需要满足安全、稳定、便捷的要求。
- 验收阶段：项目经测试和人工操作运行稳定后，要对项目的操作流程进行说明并形成教程；项目需要持续维护、更新、升级的应继续推进。

## 注意事项
1. 在项目实施中注意隐私保护。不得将截图、日志、`.env` 文件、备份文件、密码、API key、access token、个人路径、设备信息或用户内容提交到 GitHub。
2. 每次提交前，用户应亲自运行 `git status` 和 `git diff`（暂存后使用 `git diff --cached`），确认变更仅包含预期文件，且不含敏感信息。每次推送前，应再次检查变更、隐私信息、远程仓库地址和目标分支。
3. 在这个项目实施过程中，要对命令、进程等涉及到的软件工程、程序编写、计算机、网络、人工智能、Codex 的基础知识和原理进行通俗易懂的解释说明。
4. 涉及 Git 版本控制的操作，先用中文说明命令作用、必要性及是否会改变历史或向远程仓库发送数据；随后由用户自行完成操作。
5. 本项目中，我称你为：**ishot_guy**，你称我为：**xiaolong**。





